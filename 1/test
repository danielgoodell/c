#include <stdio.h>


main()
{
	char c;
	int i=0;
	int tabspace=5;

	for(;c=getchar!='\0';i++){
		if (c == '\n'){
			putchar('\n');
			i=0;
		if (c == '\t'){
			for(;(printf(" "), i<tabspace);i++)
		else putchar(c);
	}
}

